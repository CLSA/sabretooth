Questions to ask Kimberley:

Should appointments have time windows?  How much?  15 to 60 minutes? - parameterize 
  -- parameterize
  -- call back appointments right away (no matter how long ago they were) to reschedule
      -- if can't reschedule, then call back any day around same time of original appointment

Will Sherbrooke always be bilingual?  What about a french speaking operator outside of Quebec?
  -- yes and not important (french interviews can be locked to a site(s))

Call is picked up by relative or associate of participant, what then?
  -- scheduled callbacks (different from appointments, slightly less priority than appointments)

Do we need to track appointments once they have been completed?
  -- no (hurray!)
  -- track which queue an interview comes from when assigning it
