{##
 # qnaire_view.twig
 # 
 # View qnaire details.
 # @author Patrick Emond <emondpd@mcmaster.ca>
 # @see base_view.twig for parameters
 #}
{% extends "base_view.twig" %}
{% block record %}

  {{ parent() }}
  {% from 'macros.twig' import include_list %}
  {{ include_list( 'phase', phase_list|default(false), true ) }}
  {{ include_list( 'sample', sample_list|default(false), true ) }}

{% endblock %}
