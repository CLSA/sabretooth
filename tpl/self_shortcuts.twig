{##
 # self_shortcuts.twig
 # 
 # Creates a list of shortcuts which are meant to be used under any circumstance.
 # @author Patrick Emond <emondpd@mcmaster.ca>
 #}
{% extends "widget.twig" %}
{% block widget %}
<script type="text/javascript">
// make button icons
$( function() {
  $( "#{{ widget_full_name }}_prev" ).button( {
    text: false,
    icons: {
      primary: "ui-icon-arrowthick-1-w"
    }
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_next" ).button( {
    text: false,
    icons: {
      primary: "ui-icon-arrowthick-1-e"
    }
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_refresh" ).button( {
    text: false,
    icons: {
      primary: "ui-icon-arrowrefresh-1-e"
    }
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_home" ).button( {
    text: false,
    icons: {
      primary: "ui-icon-home"
    }
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_help" ).button( {
    text: false,
    icons: {
      primary: "ui-icon-help"
    }
  } )
} );

// page button functions
$( function() {
  $( "#{{ widget_full_name }}_prev" ).click( function() {
    slot_prev( 'main' );
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_next" ).click( function() {
    slot_next( 'main' );
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_refresh" ).click( function() {
    slot_refresh( 'main' );
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_home" ).click( function() {
    slot_load( 'main', 'self_home' );
  } )
} );
$( function() {
  $( "#{{ widget_full_name }}_help" ).click( function() {
    //slot_load( 'main', 'help' ); TODO
  } )
} );
</script>
<span>
  <button id="{{ widget_full_name }}_prev">back</button>
  <button id="{{ widget_full_name }}_next">forward</button>
  <button id="{{ widget_full_name }}_refresh">refresh</button>
  <button id="{{ widget_full_name }}_home">home</button>
  <button id="{{ widget_full_name }}_help">help</button>
</span>
{% endblock %}
